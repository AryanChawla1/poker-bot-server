[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "poker-bot-server"
version = "0.1.0"
dependencies = ["websockets"]

[tool.setuptools.packages.find]
where = ["."]
